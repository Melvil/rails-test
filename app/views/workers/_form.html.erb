<%= form_for(@worker) do |f| %>
  <% if @worker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@worker.errors.count, "error") %> prohibited this worker from being saved:</h2>

      <ul>
      <% @worker.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :contacts %><br />
    <%= f.text_area :contacts %>
  </div>
  <div class="field">
    <%= f.label :search_status %><br />
    <%= f.check_box :search_status %>
  </div>
  <div class="field">
    <%= f.label :desired_salary %><br />
    <%= f.number_field :desired_salary %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
