
<div class="page-header">
  <h1><%=t '.title', :default => Worker.model_name.human.pluralize %></h1>
</div>

<%= render "list" %>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_worker_path,
            :class => 'btn btn-primary' %>
